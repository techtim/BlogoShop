{
        listen => ['http://*:8080'], 
        workers => 4, 
        lock_file => '/var/lock/BlogoShop.lock', 
        pid_file => '/var/run/BlogoShop.pid', 
        user => 'techtim', 
        group => 'techtim', 
        proxy => 1
}
#       graceful_timeout => 1,
#       accepts => 0,
#       clients => 10240,
#       backlog => 10240,
#       websocket_timeout => 90
